/** 차트 시리즈/카테고리 색상 순환용 — components/ui/chart.tsx 의 chart-1~5 시맨틱 토큰만 사용한다. */
export const CHART_COLOR_VARS = [
  "var(--chart-1)",
  "var(--chart-2)",
  "var(--chart-3)",
  "var(--chart-4)",
  "var(--chart-5)",
] as const
